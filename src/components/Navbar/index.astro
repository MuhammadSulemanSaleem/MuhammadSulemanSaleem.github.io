---
import './Navbar.css';

const currentPath = Astro.url.pathname;

const navLinks = [
  { label: 'Home', href: '/' },
  { label: 'Portfolio', href: '/portfolio' },
  { label: 'Resume', href: '/resume' },
  { label: 'Contact', href: '/contact' },
];

function isActive(href: string) {
  if (href === '/') return currentPath === '/' || currentPath === '';
  return currentPath.startsWith(href);
}
---

<nav class="navbar">
  <div class="nav__content">
    <div class="logo">
      <a href="/">
        <span class="logo__icon"><img src="/android-chrome-192x192.png" alt="" width="36" height="36" aria-hidden="true" /></span>
        Muhammad Suleman Saleem
      </a>
    </div>
    <label for="check" class="checkbox" aria-label="Toggle menu">
      <i class="ri-menu-line"></i>
    </label>
    <input type="checkbox" name="check" id="check" />
    <ul>
      {navLinks.map((link) => (
        <li>
          <a href={link.href} class={isActive(link.href) ? 'active' : ''}>
            {link.label}
          </a>
        </li>
      ))}
    </ul>
  </div>
</nav>
